<body id="page-passwords-new">
  <div class="container">
    <div class="row">
      <h3 style="text-align: center"><%= link_to 'Oslaw', root_path %></h3>
      <div class="panel panel-default">
        <div class="panel-heading">Reset your password</div>
          <div class="panel-body">
          <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
            <div class="form-inputs">
              <%= f.input :username, required: true, autofocus: true %>
              <%= f.button :button, '<i class="fa fa-envelope-o"></i>&nbsp;Send instructions'.html_safe, class: 'btn btn-success', data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> Sending..."} %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</body>
