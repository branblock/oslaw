<div class="jumbotron">
  <div class="container">
    <div class="col-md-6">
      <h2>Experience the benefits of collaborative lawyering.</h2>
      <p><span><i class="fa fa-check-square-o"></i> Real-world input on legal documents.</span>
      <p><span><i class="fa fa-check-square-o"></i> Free to use the public database.</span></p><p><span><i class="fa fa-check-square-o"></i> Create private groups for only $4.99/month.</p><p style="text-transform: uppercase">Join Oslaw and start collaborating now.</p>
      <p class="future"><button type="button" class="btn btn-info btn-lg" style="margin-top: 7px"><%= link_to '<i class="fa fa-info-circle"></i> About Oslaw'.html_safe, '#' %></button> <button type="button" class="btn btn-info btn-lg" style="#fff; margin-top: 7px"><%= link_to '<i class="fa fa-cogs"></i> How It Works'.html_safe, '#' %></button></p>
    </div>
    <div class="col-md-4 col-md-offset-1">
      <p><strong>Create Your Free Account</strong></p>
      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <div class="form-inputs" style="text-align: left">
          <%= f.input :first_name, required: true, autofocus: true %>
          <%= f.input :last_name, required: true %>
          <%= f.input :username, required: true %>
          <%= f.input :email, required: true %>
          <%= f.input :password, required: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
          <%= f.input :password_confirmation, required: true %>
        </div>
        <div class="form-actions" style="margin-bottom: 15px; margin-top: 15px;">
          <%= button_tag(type: 'submit', class: "btn btn-success") do %>
           <i class="fa fa-user-plus"></i> Create My Free Account
          <% end %>
        </div>
      <% end %>
      <div>
        <p class="future" style="font-size: 12px">By signing up, I agree to Oslaws's <%= link_to 'Terms of Service', '#' %>.</p>
        <p style="font-size: 12px">
          Already have an account? <%= link_to "Sign in", new_session_path(resource_name) %><br />
          <%= link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name) %>
        </p>
      </div>
    </div>
  </div>
</div>

<div class="container">
</div>
